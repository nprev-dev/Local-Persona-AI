memory_store.db
memory.json
personality.json
aemeath_ref.wav
__pycache__/
active.json
# Whole personas tree: the JSON is user state, and the voice clips are
# copyrighted source audio. aemeath_ref.wav is already ignored for the same
# reason, but personas/voices/ held an identical copy that was not.
personas/

# generated by tools/build_ui.cjs; rebuilt before shipping
index.prod.html
# user preferences, created on first save
settings.json

# per-conversation history
chats/
